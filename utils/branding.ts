/** Transparent background — black wordmark and icon. Navbar when the bar is white / solid light. */
export const LOGO_PLAIN_SRC = "/logo_plain.png";

/**
 * White wordmark + gold icon (file may use a light plate). Use on the transparent navbar over the dark hero only.
 */
export const LOGO_WHITE_SRC = "/logo_white_bg.png";

/** Icon-only mark — favicon, browser UI, and other tiny placements. */
export const LOGO_ICON_SRC = "/logo_icon.png";
